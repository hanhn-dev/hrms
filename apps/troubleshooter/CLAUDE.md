@AGENTS.md

# Troubleshooter (Claude)

Import app modules with `@/` (`@/features/employer/picker`, `@/features/employee/access`, `@/shared/db`). Do not use parent-relative `../` paths. Same-directory `./` is allowed. Do not import `@hrms/ui`. Components are `antd` only. Icons are `@ant-design/icons` only.
