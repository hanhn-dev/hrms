---
name: track-db-updates
description: Diff TDG HRMS DB since the last run, write a Backstage changelog of what changed, and patch knowledge pages so the wiki stays usable for a knowledge refresh. Use when the user runs /track-db-updates or asks to track database updates into Backstage.
disable-model-invocation: true
---

# track-db-updates

Read `.cursor/commands/track-db-updates.md` at the hrms repo root and execute that workflow exactly. Do not shorten or skip steps.
